export class Member{
    memberId : string;
    name : string ;
    points : string;

    constructor( id:string, name:string, points:string){
        this.memberId = id;
        this.name = name;
        this.points = points;

    }
}